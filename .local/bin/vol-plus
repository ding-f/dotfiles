#!/usr/bin/env bash

if [[ $(pamixer --get-volume) == *100* ]]
then
	pactl set-sink-volume @DEFAULT_SINK@ 100%
	notify-send -t 3000 "🔊 Seriously You Fucking Deaf?"
else
	pactl set-sink-volume @DEFAULT_SINK@ +5%
fi
